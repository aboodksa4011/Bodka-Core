# Bodka Core Architecture

هذا الملف يشرح البنية العامة للمشروع بشكل قابل للمراجعة بدون الحاجة لتشغيل الملف التنفيذي.

## النظرة العامة

Bodka Core هو مشروع تخصيص لـ Discord على Windows مبني جزئياً فوق بنية Vencord ومكونات مفتوحة المصدر متوافقة. إضافات Bodka الخاصة تُبنى كوحدات مستقلة داخل طبقة الإضافات، بينما يستفيد المشروع من البنية الأساسية للمشروع الأم في التحميل والـpatching والإعدادات والتكامل مع واجهة Discord.

## الطبقات الرئيسية

1. **Upstream / Vencord layer**
   - نظام الإضافات والـpatching.
   - واجهات الإعدادات والمكونات المشتركة.
   - التكامل الأساسي مع Discord desktop.

2. **Bodka plugin layer**
   - إضافات تبدأ عادة باسم `Bodka*`.
   - تركيز على العربية، الخصوصية، الأدوات، الإنتاجية والتخصيص.
   - أمثلة منشورة للمراجعة تحت `source-review/`.

3. **Installer / distribution layer**
   - ملف `BodkaCoreInstaller.exe` لتثبيت النسخة الجاهزة للمستخدم.
   - المصدر العام المنقح أمنياً يزيل المفاتيح والأسرار قبل النشر.

## مبادئ التصميم

- أقل قدر ممكن من البيانات خارج الجهاز.
- الإعدادات الحساسة لا تُضمّن داخل السورس العام.
- وظائف الخصوصية تعمل محلياً متى أمكن.
- كل إضافة تبقى وحدة مستقلة لتسهيل الاختبار والتعطيل والتشخيص.
- عدم الادعاء أن مكونات upstream طُورت من الصفر داخل Bodka Core.

## تدفق الإضافة

`Discord UI -> Vencord plugin API -> Bodka plugin -> local logic / optional external provider -> result back to UI`

الإضافات التي تتعامل مع مزودات خارجية يجب أن تعرض ذلك للمستخدم بوضوح، وتستخدم مفاتيح يضيفها المستخدم بدلاً من مفاتيح مضمّنة في المصدر العام.

## حدود التقييم

المستودع يفرق بين:

- **كود Bodka الأصلي أو المعدل خصيصاً للمشروع**: عيناته موجودة في `source-review/`.
- **بنية ومكونات Vencord والمشاريع الخارجية**: تخضع لتراخيصها الأصلية ويجب نسبها لأصحابها.

هذا الفصل مقصود لجعل مراجعة المشروع أكثر شفافية ودقة.